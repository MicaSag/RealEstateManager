<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/Widget.MaterialComponents.CardView"
    android:id="@+id/photo_list_mcv"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:maxHeight="150dp"
    >

    <FrameLayout
        android:id="@+id/fragment_photo_list_cl"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Image of the Estate -->
        <ImageView
            android:id="@+id/photo_list_image"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:layout_marginTop="2dp"
            android:layout_marginBottom="2dp"
            android:layout_marginStart="2dp"
            />
        <!-- Delete Image Button -->
        <Button
            android:id="@+id/photo_list_bt_delete"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="delete"
            android:alpha="0.5"
            android:layout_gravity="center"
            android:visibility="invisible"/>
        <!-- Room Type -->
        <EditText
            android:id="@+id/photo_list_et_room"
            android:layout_width="150dp"
            android:layout_height="wrap_content"
            android:background="@color/photo_list_description"
            android:layout_gravity="bottom"
            android:textAlignment="center"
            android:visibility="invisible"/>
    </FrameLayout>

</com.google.android.material.card.MaterialCardView>
